def left_rotate(arr, d):
    d = d % len(arr)
    return arr[d:] + arr[:d]

# Example usage
arr = [1, 2, 3, 4, 5]
d = 2
rotated_arr = left_rotate(arr, d)
print(rotated_arr)  # Output: [3, 4, 5, 1, 2]
